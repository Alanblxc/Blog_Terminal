{
  "name": "blog-terminal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate-posts && vite",
    "build": "npm run generate-posts && vite build && node config.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "preview": "vite preview",
    "generate-posts": "node config.js",
    "copy-posts": "node config.js",
    "start": "vite preview"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "katex": "^0.16.27",
    "marked": "^17.0.1",
    "mermaid": "^11.12.2",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "gh-pages": "^6.3.0",
    "playwright": "^1.57.0",
    "vite": "^5.2.0"
  }
}
